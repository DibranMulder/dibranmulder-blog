<footer id="footer">
  <div class="footer-inner">
    <span>&copy; <%= date(new Date(), 'YYYY') %> <%= theme.wordmark || config.author || config.title %></span>
    <% if (theme.footer_note){ %>
      <span><%= theme.footer_note %></span>
    <% } %>
  </div>
</footer>
